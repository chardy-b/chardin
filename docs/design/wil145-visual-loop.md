# WIL-145 · Visual Loop 1 implementation and Loop 2 handoff

Authority: the user's WIL-145 implementation request, on base main `939b8050f79d7ed023bcf40255765f03417a4ef9`. No remote ticket was retrieved. The full skeptical review is preserved in `.hermes/execution/chardin/visual-loop-1/review-transcript.log:4100–4159`; its 3.9/10 score remains the last actual visual judgment. CPU evidence is not a replacement score.

## Design amendment

This request authorizes reauthoring WIL-124's exterior presentation. Geometry, aperture shape, ramp triangles, collider dimensions, support ownership, score ticks/endpoints, exposure and sun remain unchanged. Exterior shell/floor/edge pigments are now `#B2AB95` / `#766B59` / `#C2B99E`; the edge uses the same toon response as the shell. Ambient/hemisphere intensities are .65/.85; normal shadow bias .012. The original four-band toon ramp is 72/128/188/242 with a .06 emissive floor; the traveler shares one four-byte ramp across its palette without emission. Outline depth/normal strengths .34/.12 suppress thin meadow noise. Interior radiance remains independent, quiet and texture-free. No bloom, render-scale, DPR, shadow-map, pass, terrain-vertex or instance-count budget was increased.

Terrain pigment and normals are continuous at shared vertices, using slow spatial fields rather than face lottery colors. Three original tuft silhouettes (open cup, low fan, swept pennant) use six bowed/tapered blade profiles in each 48-triangle cluster. A common density field makes meadow masses and quiet gaps with deterministic nested tier placement. All tier instance counts remain 520/960/1520. The .28 maximum swept/scaled tuft footprint reserves the existing .20/.75-radian clearings plus margin. Roots remain on sampled terrain; no grass enters the ramp. Wind deforms one shared 144-vertex buffer with at most .032 local units of displacement, pinned roots and conservative bounds. Color/depth/normal passes see the same geometry. Reduced motion freezes all tiers at the last presented time, including subsequent quality changes; pause never advances wind.

Traveler v2 uses original elliptical ring sections, a short coat silhouette with visible leg separation, narrower chamfered pack, distinct skin/linen/leather values, elbows, knees, ankles and separately parented boots. Walk lasts .8 seconds at 1.65 units/s; run .52 seconds at 3.3 units/s. Stance fractions .5/.34 give alternating grounded walk contacts and run flight intervals. Foot trajectories are baked to joint tracks with two-link inverse kinematics. Hip movement is .012 during locomotion, idle breath .002; scarf/pack responses are small. Jump has its own clamped .7-second pose sequence and never translates the root. Model bounds remain within the reserved 1.30 height. No external character/art input or root-motion authority was added.

Actual signed displacement rates the gait, so blocked/partial/backward travel does not blindly play at full requested speed. Walk/run transitions preserve normalized phase and blend over .12 seconds without time warping. Presentation retains previous/current node transforms and reuses storage; render alpha never advances the mixer. Motor and camera simulation advance together at 60 Hz; render uses interpolated actor/camera transforms with camera obstruction rechecks. Sphere interpolation preserves radius. Small terrain seating offsets follow rendered facets and fade before ramp/takeoff; they never mutate motor support. Two original vertex-alpha contact ellipses supplement the existing sun shadow map, with no textures/targets/passes.

The exterior rig has a +.42-radian composition yaw plus a .85-unit shoulder (.48 portrait), which places the traveler beside the destination. Both fade to zero on the ramp; interior/eye/view obstruction, hiding and aperture behavior remain intact. Compact HUD surfaces align type, corners and controls; lifecycle status stays available to assistive technology without a persistent RUNNING pill. All actions, normal quality/light controls, failure/recovery/entry/pause states, focus and 44px targets remain. Tablet/mobile use two compact control rows; short screens retain scrollable disclosures and overlays.

## Controller-owned exact frames

Use a hooks-enabled build (`NEXT_PUBLIC_E2E_HOOKS=true`), `/?e2e=1`, 1280×720 CSS viewport, DPR 1, **Balanced**, ordinary motion (`no-preference`), and a freshly loaded/entered world with a loaded traveler. Await normal readiness, fonts and canvas metadata. Keep normal complete UI visible, disclosures collapsed, and do not add crops, masks, effects or hide controls.

- `world.png`: in a fresh runtime, evaluate `walkToSkyspacePoint({ point: 0, facePoint: 1 })` from `tests/e2e/helpers/skyspace-route.ts`. Call `api.present(1)` and capture at neutral light tick 0. This navigates the actual motor to the three-quarter approach. CPU projections check complete ramp corners inside the viewport and door/head separation; visibility/artistic balance must still be inspected in the PNG.
- `character-ui.png`: in a **separate fresh runtime**, evaluate `prepareCharacterFrame` from `tests/e2e/helpers/visual-loop.ts`. It runs 35 fixed steps with forward/run intent (1.925 units of curved travel), then presents alpha 1. Run phase is approximately .121795 (duration is exported Float32), with a supported left-foot stance, opposing swing and nearby meadow. Record actual returned phase; never replace this with a whole-cycle neutral frame. The helper rejects reused runtimes and unexpected phases.

Use the existing `captureManualFrame` barrier (GPU finish → one presentation barrier → full viewport PNG). Each target gets JSON from `visualLoopMetadata(api.snapshot(), { commit, dirty, diffSha256 })`. The controller supplies the actual 40-character Git commit, dirty flag and 64-character diff hash; no revision is hardcoded into runtime. Record the capture PNG hash and browser/GPU identity alongside it. Metadata includes viewport, requested device DPR, quality, motion preference, light tick/phase/playback, player position/heading/support, camera position/target/up/yaw/pitch/FOV/mode, simulation time, animation clip/phase/time scale, actual signed speed, movement intent and presented alpha/positions. Existing quality settings determine effective DPR/render scale; retain drawing-buffer dimensions in the controller's environment record.

The new `present(alpha)` seam only renders [0,1], requires both the existing build guard and manual URL, and cannot step/teleport/change support or animation time. Normal production installs no test API. Existing bounded `step`/`stepInput` and route helpers are unchanged. `present(1)` preserves deterministic manual captures independently of RAF; intermediate fractions support controller temporal evidence.

Supplemental controller validation covered ordinary walk, run, curved turn, jump/landing, and return to idle in a genuine 1280×720, 25fps, 6.76-second browser recording. The scenario passed its movement-distance gate; bounded contact-sheet inspection found no obvious camera jump, gross pose pop, clipping, or foot/ground penetration. This does not prove fine foot sliding or frame pacing and remains supplemental to the two Loop 2 primary PNGs. Controller inspection also passed the actual strict rendered baselines at 820×1180 tablet and 393×727 mobile; separate CPU framing fixtures cover 768×1024 and 393×851. Pixel snapshots remain strict (`threshold: 0`, `maxDiffPixels: 0`). The five deliberate WIL-145 baseline replacements were regenerated only after behavioral gates passed, visually inspected for corruption/clipping/responsive obstruction, and bound to updated inventory hashes. No gate, retry, tolerance or performance ceiling was weakened.

## CPU evidence and limits

`traveler-authorship.test.ts` parses the actual generated GLB, proves boot/knee/leg and hand/elbow parentage, dimensions, clip names/durations/track counts, sample finiteness, loop continuity, grounded contact drift and both-feet run flight. It also exercises actual mixer rate/phase/material conversion and exact disposal. `meadow-presentation.test.ts` checks pigment/normal continuity, profile curvature, quiet density gaps, tier counts, swept bounds, root pinning, repeated update allocation identity and freeze/disposal. `presentation.test.ts`, `runtime-framing.test.ts` and `reduced-motion.test.ts` cover fixed-state immutability, radius/support projection, camera clearance, render-only interpolation, capture recipes and tier-independent freezes. Existing route/collision/UI/loading/failure/accessibility/resource tests remain required.

These are bounded CPU invariants, not proof of imperceptible foot slide/pose transitions, GPU shadow quality, meadow beauty, full responsive reflow, contrast, screen-reader experience or frame pacing. See `.hermes/execution/chardin/visual-loop-1/implementation-result.md` for exact local runs and remaining controller gates. Existing physical-device/thermal/Safari, release/security/CI/independent review and rights decisions remain open. No browser, server, external service, commit, push or PNG generation was used during implementation.
