# WIL-146 · Loop 3 implementation and capture handoff

Authority: supplied WIL-146 request/plan and the full skeptical Loop 2 review at `/home/hermes/chardin-visual-1/.hermes/execution/chardin/visual-loop-2/review-transcript.log`, starting at line 4389. Base `0778ee06d137f4258a29885a7baf016e2857d51f`; branch `ryli721/wil-146-visual-loop-2-resolve-meadow-massing-action-silhouette-and`. No ticket service was called. **The implementation handoff retained 4.9/10; the subsequent retained Loop 3 review scored the two formal captures 5.4/10.** This browser repair preserves that observed score; source and CPU improvements are not a new visual score.

## Meadow and light amendment

Three genuinely separate meshes replace the repeated composite cluster: low broad creeping leaves (.13 maximum local height, 24 triangles), medium three-leaf fans (.27, 24), and sparse two-stem accents (.42, 16). All come from original authored bowed profiles. Species share one vertex-colored material and appear in every quality tier. Total instances remain 520/960/1520; three instanced draws replace one, with fewer submitted plant triangles. Only occupied instance slots are uploaded. No new texture allocation, pass, shadow map, DPR or render-scale increase. Per-species wind phase/amplitude is distinct, while instances within a species still share deformation. Root displacement is zero and maximum local displacement remains below .032. Bounds reserve the unchanged .28 scaled footprint. Ordinary instance rotation varies with the broad field, rather than making radial stars. Tall plants occur only inside dense patches, leaving lower broken contours at patch boundaries. A lower-frequency connected density field rejects plants entirely in quiet regions. Existing spawn/pavilion clearings and facet-root seating remain intact.

The existing four-byte terrain/plant ramp now uses linear filtering: a continuous diffuse response removes hard toon thresholds on interpolated terrain normals. Traveler palette retains the discrete four-band ramp. The two previous PNGs were actually opened during implementation; their large foreground wedges were visible. No GPU A/B diagnosis was permitted, so removal of the observed wedges remains a controller check, not an asserted result. Polygon count and support geometry are unchanged. Plant vertex pigment darkens roots continuously. Pavilion shell/floor/edge pigments are now `#B8B1A0` / `#A49A83` / `#C8C0AC`, with restrained base/roof-thickness vertex value and a muted ramp toe. The pavilion borrows the continuous terrain ramp. Interior radiance, aperture, structural triangles, collider, light score, sun direction/intensity and exposure remain unchanged. Shadow opacity is .72 and normal bias .025; outline depth/normal strengths .28/.08. These reduce harsh foreground shadow and thin-edge emphasis within Balanced's existing .85 target scale and 512 map.

## Traveler and temporal presentation

Traveler v3 has a tapered jaw, projected nose, asymmetrical swept crown/nape, shaped shoulders/hem, tapered sleeves, narrower tapered pack/flap and distinct cool trousers. Run uses forward torso lean/twist, counter-swinging arms with elbow flexion, .25 swing-foot lift and .25 lateral ankle separation. It retains .8/.52-second walk/run cadence, .5/.34 stance fractions and .33 leg segments. Swing trajectories now match stance velocity at their endpoints. Sixteen tracks per clip, 65 key samples, no root translation, no imported images. Tests parse the regenerated binary and test the actual documented .121789 run contact, contact drift, run flight and loop endpoints.

Actual tangential displacement rates gait; jump/support-normal displacement does not inflate it. Below .02 units/s, brief interruptions retain phase; sustained blocking of .08 seconds blends to Idle without changing the motor state. Fading locomotion actions share the incoming normalized clock. A presentation-only two-link foot solver captures supported stance anchors at fixed steps, samples the actual terrain/ramp/floor and interpolates targets for fractional renders. Swing feet follow sampled ground with authored clearance. The .18 reach limit and .10–.18 release blend keep old anchors from dragging a leg through a reversal or turn; airborne animation releases all plants. Sole orientation follows the sampled normal. The root and support identifier remain motor-owned; no motor changes were made. Separate shadow material opacities fade each lifted foot to zero, rather than popping a shared-strength ellipse.

Camera modes interpolate across render samples and ease fixed-step mode transitions with a .12-second time constant. History travel and sightline sweeps reject unsafe interpolation; collision safety can still select the resolved safe pose directly. FOV uses the same fixed-step easing. Reduced motion bypasses automatic camera easing and preserves the existing wind/idle/score freeze behavior. The .42 composition yaw and shoulder rig still place the destination beside the traveler. The three implementation regressions were repaired and controller browser verification passed: traveler restores after ordinary P3→P0 exit on desktop/mobile, unavailable-pavilion status no longer intercepts mobile entry, and paused canvas pixels remain exact after a GPU-finish plus one presentation-only compositor barrier. The unchanged pause scenario passed three consecutive zero-retry runs, retaining before/after PNGs and state, followed by a full zero-retry suite with 29 passes and five intentional project skips. Continuous footage beyond these focused paths and physical-device performance remain follow-up evidence.

## UI

Native quality selection, disclosure and navigation share semantic surfaces, borders, corners, chevrons and hover/focus treatments. Controls retain at least 44px targets and native keyboard selection. Guide/about controls expose their controlled panel IDs; Escape closes a reading panel and restores trigger focus. Paused reading panels expose Resume and suppress the competing pause overlay; loading/failure/recovery states remain available. Lifecycle panels reserve space below the HUD and scroll within the available short-screen height. All responsive claims still require controller inspection.

## Exactly two primary captures

Both are complete 1280×720 CSS viewport PNGs, DPR 1, Balanced, ordinary motion, neutral light tick 0, normal complete UI, collapsed disclosures, ready loaded traveler, fresh entered runtime (`NEXT_PUBLIC_E2E_HOOKS=true`, `/?e2e=1`). No crops, masks, hidden UI or added effects.

1. `.hermes/execution/chardin/visual-loop-3/world.png`: real motor `walkToSkyspacePoint({ point: 0, facePoint: 1 })`, then `api.present(1)`. Keep complete ramp connection, doorway/inner opening, traveler and substantial foreground meadow.
2. `.hermes/execution/chardin/visual-loop-3/character-ui.png`: **separate fresh runtime**, `prepareCharacterFrame()` (35 real forward/run steps), then `present(1)`. Cadence is unchanged; phase remains near .12. Record actual returned phase and signed speed. Include both feet and nearby plants.

Use `captureManualFrame` for the GPU-finish/presentation barrier and whole viewport PNG. Use `visualLoopMetadata` for source/state identity and evaluate `visualLoopEnvironment` in that same page for actual GPU/vendor/WebGL and drawing-buffer dimensions; record whether unmasked renderer data is available. Add the controller's actual `browser.version()`, PNG SHA-256, commit, dirty flag and **full** binary diff SHA-256 including sorted untracked files. No source revision or renderer identity is fabricated by runtime. Inventory hashes do not bind screenshots that have not been captured.

Supplemental ordinary/reduced-motion footage remains required for the broader idle→walk→run, reversal, blocked movement, curved turns, jump/landing, grass→ramp→floor→exit, and view/camera transition assessment. Controller browser checks covered 393×727 mobile, 820×1180 tablet and the focused responsive failure flow; 393×851, 768×1024 and 844×390 CPU contracts do not substitute for rendered artifacts. Existing strict `threshold: 0`, `maxDiffPixels: 0` remained unchanged. Five deliberate WIL-146 baseline replacements were generated only after behavioral checks and inspected for corruption, missing content, clipping, penetration and responsive obstruction; their exact hashes are in `docs/asset-inventory.json`. No implementation-time browser, external download/service, commit or push was used.

## Provenance

First-principles Chardin work only. Messenger inspiration is limited to broad vegetation rhythm, readable negative space and restrained motion; no Messenger or Turrell asset, code, shader, texture, model, sequence or layout was imported, traced or adapted. No dependencies added. Current exact hashes are in `wil146-provenance.json` and the inventory; older provenance file entries remain current-byte records with explicit successor notes, while their descriptions document historical work. Legacy showcase/template rights, dependency notices, human ownership and repository licensing decisions remain unchanged and unresolved where previously recorded.

## Full-browser regression repair

The retained full-browser run failed desktop/mobile ordinary-motion exit visibility, mobile paused canvas equality, and mobile landmark-failure entry. The repair separates architectural hiding from rendered camera proximity: the unchanged reserved capsule has .49 entry and .59 restoration clearance, independent of convergence toward a moving camera destination. Camera travel/sightline sweeps, motor, compact interior rig and route remain unchanged. The CPU route now completes the score and P3→P2→P1→P0 exit with ordinary motion throughout, also after pause, quality selection and context reconstruction.

Pause retains the displayed fractional presentation sample on redraws. Identical resize/media notifications do not re-resolve the camera or recapture feet; a real quality change samples the new surface at the frozen phase. Wind has its own fixed simulation clock and does not accumulate hidden elapsed motor time during reduced motion. The grass generator owns geometry only; it still receives an explicit phase, and all three species/tier buffers retain that phase. Equal FOV endpoints bypass interpolation to avoid projection-matrix roundoff drift. Light score ticks remain fixed-step owned; paused redraws never advance them. Explicit still/light selections and Return to clearing remain intentional user actions.

Unavailable-pavilion status now shares the welcome/pause/recovery panel's normal flow and scroll area with actions, without absolute positioning or pointer-event suppression. During play, the existing live experience status announces availability and Light and view retains the visible explanation. CPU component tests evaluate production responsive CSS flow/overflow/44px contracts at 393×851, 393×727, 768×1024, 844×390 and 1024×600; JSDOM does not measure layout rectangles.

No formal PNG or comparison baseline is changed by this repair. Both retained formal recipes match their captured state and the before/after CPU scene-input hashes. Strict `threshold: 0` and `maxDiffPixels: 0` remain unchanged. No browser, Playwright, Next server, external service, commit, push or WIL-147 world/skyspace expansion is part of this repair. Actual canvas pixels, continuous motion and responsive hit-testing still require the controller's unchanged browser gates. Detailed local evidence and limits: `.hermes/execution/chardin/visual-loop-3/browser-repair-result.md`.
